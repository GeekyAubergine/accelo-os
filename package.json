{
  "name": "accelo-os",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:client": "vite",
    "dev:server": "bun run src/server/index.ts --watch",
    "build:client": "tsc && vite build --host",
    "preview": "vite preview"
  },
  "devDependencies": {
    "bun-types": "^1.0.12",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}
